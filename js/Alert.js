function validation()
{
    swal(
        'The Internet?',
        'That thing is still around?',
        'question'
        );
}
         
function basicMessage(text)
{
    swal(text);
}

//https://sweetalert2.github.io/